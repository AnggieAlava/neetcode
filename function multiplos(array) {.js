function multiplos(array) {
  // return array.filter((num, index) => (index === 0 && num === 0) || (index !== 0 && num % index === 0))
  // return array.filter((num,index) => num % index === 0 && index !== 0)
}

console.log(multiplos([22, -6, 32, 82, 9, 25]));
